spring:
  datasource:
    url: jdbc:h2:mem:dododb
    username: user
    password: pswd
  jpa:
    show-sql: true
#    hibernate:
#      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2
server:
  port: 8080